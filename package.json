{
  "name": "cloud-run-dsh",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --build",
    "test": "bun test",
    "build": "tsc --build",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
