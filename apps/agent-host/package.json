{
  "name": "@cloud-run-dsh/agent-host",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent Host \u2014 runs inside Cloud Run Instance (skeleton)",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "tsc --build"
  }
}
